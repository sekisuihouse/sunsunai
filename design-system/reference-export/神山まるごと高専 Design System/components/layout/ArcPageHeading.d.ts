/** Giant-arc page title with vertical text (admission-page motif). */
export interface ArcPageHeadingProps {
  /** black-boxed label, e.g. 2027年度 */
  label?: string;
  /** vertical main title, e.g. 入試情報 */
  title: string;
  /** px, default 560 */
  height?: number;
}
