Scroll-reveal wrapper reproducing `.js-fade` / `.is-show`: content fades up 15px over .8s ease-in-out when it enters the viewport. Respects prefers-reduced-motion.

```jsx
<RevealMotion><h2>スタートアップの学校</h2></RevealMotion>
<RevealMotion delay={150}><p>本文…</p></RevealMotion>
```
